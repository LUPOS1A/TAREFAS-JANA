#include <stdio.h>

int main() {
    int v[10], maior;

    printf("Digite 10 números:\n");
    for (int i = 0; i < 10; i++) {
        scanf("%d", &v[i]);
        if (i == 0 || v[i] > maior) {
            maior = v[i];
        }
    }

    printf("O maior número é: %d\n", maior);
    return 0;
}
